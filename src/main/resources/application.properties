server.port=8080
spring.application.name=martflow

# MongoDB is NOT auto-configured by Spring here. We use the raw mongo-driver-sync
# and read MONGODB_URI / MONGODB_DB from the environment via com.martflow.persistence.Config.
logging.level.com.martflow=INFO
